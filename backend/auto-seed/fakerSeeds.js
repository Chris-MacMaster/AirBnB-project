// const { faker } = require('@faker-js/faker');
// const bcrypt = require('bcryptjs');

// //simple random number generator
// const rNum = (num) => Math.floor(Math.random() * Math.floor(num) + 1)


// //Start

// const seedUsers = (num) => {
//     let users = new Array(num).fill("")

//     for (const i in users){
//         users[i] = {
//             username: faker.internet.userName(), 
//             email: faker.internet.email(), 
//             hashedPassword: bcrypt.hashSync(faker.internet.password())
//         }
//     }

//     //we get pack array of 100 populated that way

//     return users
// }


// module.exports = {seedUsers}




//we're passing in KVPs, but we could make the array anywhere, can import it from somwhere else


/*
could have the array be a product of a function

see start


*/